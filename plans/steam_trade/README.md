# Steam 交易接入 · 跨会话进度总览

> 目标:孵化结果全部经 Steam Inventory Service 记录为玩家 Steam 资产;宠物可在 Steam 交易。
> 本目录是**唯一进度事实源**。任何会话(人或 AI)开工前必读本文件 + 所认领工作流文档;设计层面的疑问以 [00-decisions.md](00-decisions.md) 为准。

## 状态看板

| WS | 工作流 | 文档 | 状态 | 依赖 | 备注 |
|----|--------|------|------|------|------|
| WS1 | 创建应用与网站配置 | [01-app-setup.md](01-app-setup.md) | ✅ 完成(2026-07-12) | — | **App ID 4956830**;库存服务已启用;掉落参数测试值 5/10/5 已发布 |
| WS2 | Rust 集成与状态机 | [02-rust-core.md](02-rust-core.md) | ✅ 代码完成(2026-07-12) | — | cargo test 47 通过;真机冒烟移交 WS4 |
| WS3 | 物品目录/脚本/图标 | [03-itemdefs.md](03-itemdefs.md) | ✅ 完成(2026-07-12) | — | **75 条 itemdefs 已上传生效**;图标 27/27 已渲染(icon_url 待图标推送 GitHub 后重传) |
| WS4 | 联调与交易验收 | [04-testing.md](04-testing.md) | 🚧 冒烟 A5-A7 完成(2026-07-16),待 app E2E | Steam 客户端 + `GULUGULU_STEAM=1` 真机跑 app(动真实存档,须用户执行) | set:/自升阶/并集/窗口掉落 原语已真机实证;剩 UI 全流程 + 隔天窗口重置观察 + A8(90001-90007 defs + 101×2 残留,`steam_smoke::a8_cleanup_residue` 一键清) |
| WS5 | 上架发布 | [05-release.md](05-release.md) | ⛔ 冻结 | 用户明确 go-ahead | **用户指示:先测试,不正式发布** |
| WS6 | 融合2.0目录 + 铸造上架(P5) | (本 README 进度日志 + 00-decisions 各 2026-07-15/16 节) | ✅ 完成(2026-07-16) | — | **783 条目录已上架**(标签/自升阶/57 并集 gen/24 商店窗口 gen);铸造/collect 重接线代码落地(cargo 99 绿);创意工坊代码就位(公开可见性归 WS5) |
| WS6-Store | 商店页素材/文案 + 全局英语本地化 | [06-store-page.md](06-store-page.md) | 🚧 素材文案已产出,待用户审阅→后台填写 | 用户登录 partner 站(👤);提审/工坊公开归 WS5 | **全套胶囊/库图/图标 + 12 张双语截图已出**;中英双语文案定稿;**全局英语本地化落地**(前端 i18n + Rust 消息键,`npm run build`+`cargo test` 双绿);拍板:仅 Windows / $0.99·¥6 / 开工坊 / Mobi Studio / steamdev@mobigames.cn |
| WS7 | 云存档同步 | [07-cloud-save.md](07-cloud-save.md) | ✅ 代码完成+真机验证(2026-07-21) | — | **Cloud API 三件套同步**(存档+账本+语录)·较新者胜+备份·清档夺权;**后台配额早已配好、4 冒烟全绿、运行中 app 已推三件套上云(persisted)**;发行前取消「仅开发者云」勾选(05-release R9,👤) |

状态图例:⬜ 未开始 · 🚧 进行中 · ⛔ 阻塞/冻结 · ✅ 完成

## 依赖关系

```
WS1(App ID + Inventory Service 开通)──┐
WS2(Rust/前端代码,不等 App ID)───────┼──► WS4(联调+交易验收)──► WS5(上架,冻结中)
WS3(itemdefs 生成;上传一步等 WS1)────┘
```

## 会话约定(必读)

1. **开工**:读本 README + 认领的 WS 文档;把看板对应行改成 🚧 并在"备注"写明会话/日期。
2. **完成任务**:在 WS 文档把 `- [ ]` 改 `- [x]`,行尾追加 `(YYYY-MM-DD,一行结果)`;整个 WS 完成后更新看板为 ✅。
3. **改设计**:先改 [00-decisions.md](00-decisions.md)(附理由与日期),再动代码。**安全不变量与 itemdefid 编号一经使用不可回改**。
4. **受阻**:看板标 ⛔ 并在 WS 文档写明阻塞原因与需要谁解除。
5. **验证门**:任何代码合入前跑 `npm run build`(projects/gulugulu-app)+ `cargo build && cargo test`(src-tauri)。仓库无 lint/test 之外的静态门。
6. **安全红线**(对所有会话生效):支付、签署协议、输入任何密码/密钥必须用户本人;合作伙伴网站"保存/发布"类不可逆点击须先经用户确认;全程**不创建、不经手** Steam Web API key;WS5 冻结期间不得执行任何发布动作。

## 进度日志

- 2026-07-11 计划批准(v2:随机生成器接口 / 先测不发 / 跨会话文档),本目录建立。WS1 于同会话启动。
- 2026-07-11 用户完成 $100 支付;创建应用 **Gulugulu(App ID 4956830)**;启用库存服务,掉落参数设测试值 5/10/5。
- 2026-07-12 配置发布成功(仅所有者可见)。WS1 A/B 完成;WS2、WS3 解锁,可并行认领。
- 2026-07-12 主会话完成 WS2(Rust/前端全量,cargo test 47 通过)+ WS3(75 条 itemdefs 上传生效,27 图标渲染)。WS1 全部完成。**下一步 = WS4 联调**(需 Steam 客户端 + 拥有 4956830 的账号);待决事项清单见主会话汇报(或 00-decisions.md 待决节)。
- 2026-07-12 **用户指示:本地调试期关闭 Steam 集成** → 加总开关(默认关,`GULUGULU_STEAM=1` 或改 `STEAM_DEFAULT_ENABLED` 重开,详见 00-decisions.md「集成总开关」)。关闭时全部玩法回退纯本地逻辑。门禁复跑全绿(cargo test 47 / cargo build / npm build)。**WS4 暂缓,等用户本地调试完成后重启。**
- 2026-07-14 **用户拍板两项新机制**(纯 Steam、无自建服务器)→ 记入 00-decisions.md「用户拍板(2026-07-14)」:①孵化率挂钩全局存量 = **手调 generator 权重**(承接「热调 itemdef」不变量,不做客户端概率耦合);②AI 物种形象/名字由首个生成者上传 = **创意工坊 UGC**(按 petId 全局共享、跨交易保留,解决待决 1、化解待决 4 外观面)。均为**研究+决策**,暂未进入实现。
- 2026-07-15 **WS6:融合2.0 Steam 预创建 + 创意工坊落地(准备+暂存,不触真机)**。①**确定性槽位身份**(`fusion_slots.rs`↔`fusionSlots.ts` 加 `recipeOrdinal`/`aiItemDef`/`slotCodename`,替换随机 `aif{6hex}`;`verify_fusion_slots.mjs` 逐位对拍绿)。②**加法编号**(永不重编旧 101-521):57 新固定宠 `601-657`(config `steamItemDef` 已写两份、`game_config.rs` 冻结测试改断言);570 AI 占位 `10000 + 序号*100 + slot`(codename=`aif`+2位序+2位槽,同为创意工坊 petId 标签)。③`generate_itemdefs.mjs` 产 **702 itemdefs**(仅宠物条目,新蛋/生成器龙头链推迟——3+元素并集 exchange 未解,§9 P5 待决 2);`render_steam_icons.tsx` 出 **63 上架固定物种** + 57 张 AI 占位图(**每配方一张:品质底色 2..6元素=绿蓝紫橙红 + 中央未知宠物黑影 + 该配方元素象牙白勋章花环压在其上**,复用 ElementIcon 的 BADGE_PATHS) = 120 PNG;**旧 21 只二阶兼容物种(tier2)不再产 Steam 图标**,`generate_itemdefs.mjs` 的 `icon_url` 也只给 63 上架物种(legacy 201-221/301-321 保留定义但无图标,避免 404);新增 `verify_itemdefs.mjs`。④**创意工坊 UGC 代码**:`steam_workshop.rs`(高层 `Client::ugc()` 发布/查询/下载,最早发布者胜)接入 `steam.rs` 泵线程(独立 workshop 通道,全 gated);`fusion_gen.rs` 生成即用确定性 codename、生成后 best-effort 上传认领、生成前查询命中则下载复用。门禁:`cargo test` 92 绿 / `cargo build`(check)绿 / `npm run build` 绿 / 两 verify 脚本绿。**真机上传 702 目录 + 开启创意工坊 = 冻结区,待用户 go-ahead(先推图标到公开仓库,再 `--with-icons` 重传)。**
- 2026-07-15 **P5-待决②/① 定案(研究+决策,未实现)** → 记入 00-decisions.md「用户拍板(2026-07-15)」。实证 Steam exchange 语法(tag 匹配 `tag:val*N`、`;` 多备选、无跨 material/否定/消耗剩余)与每实例标签跨交易保留。**方案**:并集 exchange 多对多用「每宠一个 `set:<集合键>` **整体标签** + 每多元素配方一个 hidden generator 枚举**全部并集对** `{set:A,set:B∣A∪B=S}` + 加权 11 槽 bundle」解决 → **物种稀有度服务器精确强制**(持 A、B 两只有且仅有 `A∪B` 那个 generator 接受);同物种走宠 def 自带 `sp:<codename>*2` 确定升阶。**阶数不登记到 Steam**(纯本地,承接待决4,`tier:T*2` 方案因逼出 3000+ def 且冗余而弃),itemdef 不按阶翻倍。用户**接受**代价:融合 AI 变种 per-user 阶梯(FusionRecipeSlots)在 Steam 权威下降级为**全局加权池**(稀有度曲线保留、解锁门控丢失,本地阶梯保留为 Steam-off 模型/展示预测)。新增 57 条并集 generator(提案 20000–20056,未上传未冻结);633 宠 def 加 `set:`/`sp:` 标签 + `sp:*2` 自升阶 exchange。**硬门**:6 元素配方 364 条 exchange≈30KB,串长上限 P5 开工前须真机 `ExchangeItems` 冒烟。均为决策,暂未进入实现。
- 2026-07-15 **商店蛋全局池 + 商店宠可交易(本地已实装 + Steam 写文档)** → 记入 00-decisions.md「用户拍板(2026-07-15)· 商店蛋全局池」。**本地已实装**:蛋池去 `dexObtained` 解锁门(可产未解锁固定种)+ 只出固定配方物种(不含 AI 变种)+ 保留元素数/权重(`economy.rs` `egg_pool_candidates`/`egg_pool_push`/`roll_egg_species` 去 `save` 参、`config.ts` 镜像、tests/verify_economy 改绿;**cargo test 92 绿 / verify_economy 绿**;⚠️`npm run build` 有 36 个 `customSpecies.tsx` 报错属并行会话遗留、非本次)。**Steam 侧待 P5**:商店宠改可交易 → 每(元素×蛋阶)一个静态 generator(全局固定池天然可表达,~18-30 条,提案 21xxx);collect 改经 generator 铸造、物种取实发 def;**⚠️硬点**:金币 Steam 看不见,"花金币铸可交易宠"破水龙头不变量 → 推荐限频 generator(服务器封顶、金币建议性),或消耗低阶宠锚定;P5 拍板 + 冒烟。
- 2026-07-15 **用户选「限频 generator」+ 定每日上限 + 商店蛋游戏内上限已实装** → 记入 00-decisions.md「每日上限定案」+ [EconomyScaling.md §7.5](../../docs/gdd/EconomyScaling.md)。取向**宁可卡到极限手工玩家**(远低于孵化屋物理吞吐 384/192/96)。**商店蛋每元素·每阶 = 1/2/3/4 阶 `50/20/8/3`**(`eggDailyMintCaps`),`drop_interval` 1728/4320/10800/28800s;**游戏内已实装**(`logic_buy_egg`+`DailyCounters.egg_mints` 计数拒绝+toast、`game_config.egg_daily_mint_cap`、TS 全镜像;**cargo test 93 绿/verify_economy 绿/npm build 绿**)。**融合 generator 每配方按结果元素数 = `5/2/2/1/1`**(2/3/4/5/6 元素),依据融合被点击额度死限;**融合游戏内上限镜像待与 Steam 铸造侧一起实装**。
- 2026-07-15 **改口径:只做客户端每日上限,不做 `drop_interval`**(用户拍板)。理由:`drop_interval` 把铸造摊到全天逼挂机;**纯每日封顶让玩家 burst 肝完再下线**。**代价(接受)**:每日上限只客户端强制→对改客户端者建议性(不变量延伸),作弊者可超额;对账仍收敛,市场若被灌可后补 `drop_interval`。一阶被动掉落 faucet 保留限频。上一条的 `drop_interval` 数值**作废**,只留每日上限数(商店 50/20/8/3、融合 5/2/2/1/1)。**融合游戏内上限"等会一起做"**。商店蛋 code 本就纯客户端每日封顶(无 drop_interval),已符合本决定。
- 2026-07-16 **定案改用 Steam「24h 窗口封顶」服务器强制(撤销上一条"仅客户端")**。实证 playtimegenerator 有 `drop_interval`(游戏时长阈值)/`drop_window`(重置窗口)/`drop_max_per_window`(窗口内几个,**封顶 10**)/`drop_limit`(终身)。**方案 = `drop_interval:0`+`drop_window:1440`+`drop_max_per_window:N` → 按天封顶、窗口内 burst 领完、次日重置、服务器强制**。硬约束 N≤10 → **商店 1/2 阶由 50/20 降到 `10/8`(选 A)**,最终 **商店 `10/8/6/3`、融合 `5/2/2/1/1`(全 ≤10)**。**代码已改**:`config.json`/`config.test.json`(test 值[4,3,2,1])`eggDailyMintCaps`=`[10,8,6,3]`、`game_config` default、`config.ts` default、`types.ts` 注释;客户端计数拒绝逻辑不变(是 Steam 窗口的镜像,本地先拦)。⚠️ 共享 Rust 树仍被并行 `custom_rig` 压红,无法即时复跑 cargo test,但改动仅数值(我的 tier-4=3 测试不受影响)。文档 [EconomyScaling §7.5](../../docs/gdd/EconomyScaling.md)+00-decisions「每日上限定案」+「已查证事实」已同步。**融合游戏内上限仍待与 Steam 铸造侧一起做**。
- 2026-07-16 **P5 开工·真机上传冒烟(App 4956830)= 两大操作性未知全绿**。用户登录 partner 站,我用页面内 `fetch`+`FormData` POST 6 条冒烟(90001-90006)到 `/apps/inventoryserviceitemdefsupload/4956830/`。①**6 元素并集 exchange 实测 20854 bytes/364 条 → Steam `success:1` 接受**(回执完整回显)→ **并集 generator 方案成立、5-6 元素不回退,P5-待决② 硬门解除**。②**上传是 merge/upsert**:75→81,旧目录全保留 → 增量上传安全。技术:`window.g_sessionID` 取 sid、`fetch` 同源 POST。**已写「已查证事实」**。待运行时验(WS4,需 Steam 客户端跑 app):`drop_window`/`drop_max_per_window` burst 行为、`set:` 标签 ExchangeItems。⚠️ 6 条冒烟(90000段)留 live 目录待清理。**下一步:生成融合 2.0 全量目录(并集/商店 generator+标签+窗口参数)+ Rust 铸造/collect 代码**。
- 2026-07-16 **P5 主体落地:783 条全量目录上架 + 每日上限全实装(本地+Steam)+ 应用级窗口 1440(待发行)**。①**目录重构**:构建逻辑抽成 `scripts/steam/build_itemdefs_core.mjs`(纯函数,Node 写文件与浏览器上传共用同源 → repo↔live 零漂移);产 **783 条** = 旧 75(宠物加标签)+ 601-657/AI 570(标签+自升阶)+ **57 并集 generator(20000-20056)** + **24 商店 generator(21011-21046)**;`verify_itemdefs.mjs` 扩断言(标签/自升阶/并集对逐条复算/窗口参数)绿;`fusionSlots.ts`↔`fusion_slots.rs` 加 `UNION_GEN_BASE`/`unionGenDef` 对拍绿。②**上架**:浏览器内注入核心+seed 一次 POST,"Modified 756/783"(27 条旧生成器逐字未变跳过)+ 商店 gen 修正重传 24/24;live **789 条**(+6 冒烟夹具);存储态抽验 601/20056/21011/219/10001 全对。③**per-def 掉落字段对照实验**(90002/90007):`drop_window` 非 per-def(剥)、`drop_interval` 非零留存、`drop_max_per_window` 留存 → 方案改「应用级窗口 1440 + per-def interval:1+max_per_window」,`inventory/inventory_playtime_minimum` 已存 1440(现 5/10/1440)。④**融合每日上限实装**:config `fusionDailyMintCaps=[5,5,2,2,1,1]` + `DailyCounters.fusion_mints`,检查在 `logic_validate_fusion_pair`、计数在 `consume_fusion_pair`/`apply_fusion_local`(全路径),Rust/TS 镜像+测试;修正文档「融合 generator 挂窗口」旧表述(exchange 型无 drop 字段,融合上限=客户端 pacing,供给安全=材料净−1)。**门禁全绿:cargo 97 / tsc 0 错 / npm build ✓ / 三 verify ✓**。⑤**遗留两件**:应用级窗口须在发布页手点「准备发布→STEAMWORKS→完成发行」(合成点击对该页无效,👤 用户);WS4 运行时冒烟(需 Steam 客户端,清单见 04-testing A5-A8)。**Rust 铸造/collect 重接线(fuse→并集 gen、商店蛋 Steam-first)有意延后**——依赖 set: 标签 ExchangeItems 的运行时行为先经 A6 验证,防止把整条融合流建在未验证原语上。
- 2026-07-16 **WS4 运行时冒烟 A6/A7 全绿(用户发布 1440 窗口后)+ mint 靶向 bug 修复**。①新增 `src-tauri/src/steam_smoke.rs`(`#[ignore]` 手动集成测试,直连 SteamAPI,跑法见文件头;测试自带 Consume 清理)。②**A6/A7 一次通过**:set: 标签配对兑换 ✓(90006→90005)、item 型 def 自带 exchange 同种升阶 ✓(101×2→新101 净−1)、**生产并集 gen 20014 开池 ✓(101+104→615 泡澡獭)**、负例集合不符 → `k_EResultFail` 服务器拒 ✓ = **物种稀有度服务器强制实证,铸造重接线前置门开**。③**A5 部分通过**:首会话 13 轮零掉落 → 新会话立刻连掉 = **游玩时长短会话滞后入账、会话结束才上报**(生产要按此理解);`drop_interval:1` 每分钟节奏实证;**掉落同 def 自动堆叠(同 item_id 加 quantity)→ 一宠一 id 绑定模型需拆栈(TransferItemQuantity),列入重接线设计输入**;ConsumeItem 每次 q=1,残留 101×2 并入 A8;满 10 封顶+次日重置为隔天观察项。④**修复潜伏 bug**:outbox mint 巡检直拿宠物 def(101-106)打 `TriggerItemDrop`(必空手,从未真机跑过所以未暴露)→ 改为触发 tier1 商店 gen `shop_gen_def(1,def)`=21011-21016(interval:1/cap10,bundle 单条目保证掉出 op.def);`fusion_slots.rs`↔`fusionSlots.ts` 新增 `SHOP_GEN_BASE`/`shop_gen_def` 身份函数+测试。⑤**下一块 = Rust 铸造/collect 重接线**(原语已验,设计在 00-decisions):fuse Steam 路径改 ExchangeItems(同种→自 def / 异种→20000段,结果物种取实发 def,AI 槽未注册→触发生成)、商店蛋 collect 改 TriggerItemDrop(21xxx) Steam-first(需 EggInstance 加 shopElement 字段记录购买元素)、legacy 3XX/5XX 流退役 + `LEGACY_TIER2_DEFS` 导入转译(PR-4)。
- 2026-07-16 **铸造/collect 重接线全量落地(P5 代码面完成)** → 详见 00-decisions 新节「铸造/collect 重接线已实装」。①融合 Steam 路径:同种→自 def `sp:*2`(AI 变种→槽 def;legacy→canonical 收敛)/异种多元素→并集 gen 20000 段/异种单元素→canonical;物种取实发 def;未注册 AI 槽→`forced_codename` 锁槽挂起生成;**结果宠物物品直接绑蛋,收取纯本地**。②商店蛋 2 阶+ collect Steam-first(`NeedDrop`→TriggerItemDrop 21xxx→拆栈→实发 def 落宠);一阶维持本地先行+MintTier1。③新原语:`ensure_distinct_item` 拆栈守卫、`register_ai_slot` 乱序槽 "" 占位、`species_codename_for_def` AI 段反解、`ai_def_for_codename`/`codename_for_ai_def` 双向身份(Rust↔TS 镜像)。④`apply_fusion_local` 按亲代阶费/阶(去 tier2 硬编码);`EggInstance.shopElement`/`PendingFusionInfo.forcedCodename` 新字段(serde default 兼容)。⑤旧流兼容:3XX 蛋照走 CollectT2;意图重放按 def 分流,旧存档 outbox 直接可放。**门禁:cargo 99 绿(含 2 新测试)/ tsc 0 / npm build ✓ / 三 verify ✓**。**剩余:WS4 真机 E2E(app UI 全流程)+ 隔天窗口重置观察 + A8 清理 + WS5 冻结区。**
- 2026-07-17 **WS6-Store:商店页素材/文案产出 + 全局英语本地化落地(准备,未触后台)** → 详见 [06-store-page.md](06-store-page.md)。用户拍板(2026-07-16):平台**仅 Windows**、定价 **$0.99 / 国区 ¥6**、**开启创意工坊**、对外署名 **Mobi Studio**(不出现公司全称)、支持邮箱 **steamdev@mobigames.cn**、截图英文默认组 + 中文页覆盖。①**全局英语本地化**:新建 `src/i18n/{core,species,messages,backyard,shell}.ts` + `src/useT.tsx`(LanguageContext);前端后院/面板/壳层/教程/教练全部硬编码中文抽进域词表(zh 逐字节保留、en 玩本语种 AI 梗);Rust 用户可见 toast/错误改「`#key|arg=v`」消息键协议(`i18n/messages.ts` 的 `localizeGameMessage` 统一渲染,~70 键双语),`game/tests.rs` 断言改键;84 物种英文图鉴文案。**门禁:`npm run build`(tsc 0)+ `cargo test` 102 绿**。②**素材管线**(离线矢量直出,可复现):`scripts/gen_wordmark.tsx`(opentype.js 解 @fontsource woff → "Gulugulu"/"咕噜咕噜" 矢量路径,栅格零字体依赖;ZCOOL KuaiLe OFL 免费商用)→ `scripts/render_store_assets.tsx`(主/头/小/竖胶囊 + 页背景 + 库胶囊/头图/3840×1240 无字 Hero + 透明 logo + 184 JPG + 256/512+ICO,尺寸断言)→ `scripts/capture_store_shots.mjs`(PORT=4189 自起 vite + headless Chrome `--headless=old` + `--virtual-time-budget` + 预热重试,`src/preview/shotParams.ts` 的 `?ui/?panel/?lang/?seed/?shot` 仅预览模式驱动,`src/game/mockSeeds.ts` 富存档种子)。产物落 `assets/steam-store/`(店面图 + `screenshots/{en,zh}/` 各 6 张后院镜头,拼版 `_review_sheet.png`)。③**文案**:`06-store-page.md` 中英短简介/详细介绍(各语种 AI meme 梗高密度)、系统需求、AI 披露双语稿、工坊文案、定价、后台核对清单。④**待用户**:审阅素材 → 登录 partner 站(👤,我用文本工具驱动填写)→ **提审/工坊公开点击用户本人**(WS5 红线);desktop_hero 真机镜头待约时机 + 重编译含本地化的 release exe。
- 2026-07-16 **WS4 真机 E2E(用户真存档 + `GULUGULU_STEAM=1` 实跑)= 全链路过 + 抓修 2 个真 bug**。✅ 已验:连接/owner/迁移扫描、MintTier1 铸造(修复后 21xxx 靶向,真实道具 ×2)、商店蛋 Steam-first 收取 ×4(4 元素池全部落对,真实道具)、融合 t1×t1 ×2(并集 gen 掷中 AI 槽 1/2,`forced_codename` 锁槽生成+改名闭环)、融合蛋纯本地收取、失败原子回滚 ×5(零消耗)、**对账剪除死绑定+墓碑保等级**(pinefawn L20)、每日计数(fusion/egg mints)全记账。**🐛 修复 1(tier)**:`apply_collect` 曾按物种自带 tier 覆盖蛋阶 → 2 阶蛋掷中目录一阶物种错落 t1;改 `tier = egg.tier.max(1)`(蛋阶=宠阶)+ 回归测试。**🐛 修复 2(并集键错靶)**:融合 Steam 阶段 1 误用旧 `fusion_pair_recipe_key`(双亲**主元素对**)→ 单元素物种恰好等于并集(t1 全过掩蔽),多元素/AI 变种全部打到错误生成器(aif0602×aif0401 → 20000 而非 20039)→ `k_EResultFail`;改 `fusion_result_recipe_key`(并集键)。诊断法:eprintln 埋点 + 冒烟复现(同参兑换在 harness 成功 → 定位 app 侧参数错)+ 库存快照对账(发现死绑定)。**⚠️ 游玩时长滞后再证**:app 会话内商店蛋收取遇「窗口已满/时长不足」= 该特性,重启 app 或稍后重试即好。cargo 101 绿。
- 2026-07-21 **WS7 云存档同步(Cloud API,代码面完成)** → 详见 [07-cloud-save.md](07-cloud-save.md) + [SteamCloudSync.md](../../docs/gdd/SteamCloudSync.md) + 00-decisions「云存档定案」。机制拍板:**Cloud API(`ISteamRemoteStorage`,steamworks 0.13 高层 `RemoteStorage`,非 Auto-Cloud)**;同步**三件套**(`gulugulu-save/progress/quotes.json`);冲突**较新者胜 + 永久备份**(存档加**加法字段** `cloud_revision`(每次 `with_save` +1)/`cloud_force_push`,**不 bump v8**,跨版本安全)。①新模块 `steam_cloud.rs`(封装 + 纯判定 `decide_cloud_action` + 8 单测);②`steam.rs` 连线一次性 `cloud_pull_reconcile`(锁内复比修订号,采纳前备份 `.pre-cloud-*`) + `pump_loop` 30s `cloud_push_pass`(哈希判变化) + `SteamCall::CloudPush`/`cloud_push_now`/`steam_cloud_sync_now`;③`codex_adapter::replace_progress_store_bytes`(账本首见 EOF 播种 → 整份覆盖不重算);④**清档夺权**:`debug_clear_save` 置 `cloud_revision=prev+1`+`cloud_force_push=true`+连线即推,杜绝「清完档又被旧云拉回」(用户测试主诉求);⑤前端 `SteamStatus` 云字段 + 市场面板 `☁️` 指示 + 隐藏/卸载 flush。**门禁全绿:cargo test 167 / npm run build ✓**。**剩:partner 站开云配额(👤 不可逆)+ 真机 E2E(冒烟/两实例/清档重启,清单见 07-cloud-save.md D)。**
- 2026-07-21b **WS7 云存档真机验证通过 + 抓修 gating bug**（用户「配好后台并测试」）。①**后台核实已配好**（浏览器进 partner 云页 4956830）：`ufsQuota=100000000`(100MB)/`ufsFiles=1000`/`ufsHideInClient=☑`(仅开发者云)——皮肤工坊留存，无需再动。②**🐛 抓修 `cloud_available` 过严**：`cloud_enable_probe` 实证 `is_cloud_enabled_for_app=false`（未发行/开发者云配置下恒 false，`SetCloudEnabledForApp(true)` 也翻不动）**但 `FileWrite/FileRead/FilePersisted` 全成功、文件真上云** → 原 `app && account` 双门会让云同步开发期静默失效；改为**只认账号级 `is_cloud_enabled_for_account` + 连线 `opt_in_app_cloud` best-effort**（`steam_cloud.rs`/`steam.rs`）。③**4 冒烟全绿**（`steam_smoke::cloud_*`，`#[ignore]`，真 Steam+账号 76561199838336217）：`cloud_save_roundtrip`（往返一致+persisted 63B）、`cloud_enable_probe`（诊断）、`cloud_clear_save_dominance`（**测试主诉求**：旧档 rev500 在云→清档 rev501+force_push→判 PushLocal 不采纳旧云→云被清空档覆盖不回滚）、`cloud_list`（只读）。④**真机 app 端到端实证**：tauri:dev 监视器已把改动热编进运行中的 app（存档 cloudRevision=5139），`cloud_list` 显示 app 已把 `gulugulu-save/progress/quotes.json` 全推上云、`persisted=true`（278805B）。⑤发行前须取消 `ufsHideInClient` 勾选（05-release R9，👤）。门禁：cargo test 167 绿 + 新增 4 个 `#[ignore]` 云冒烟。
