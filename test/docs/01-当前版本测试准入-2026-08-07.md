# 当前版本测试准入审阅（2026-08-07）

状态：已完成测试设计准入；尚未执行本轮全面测试

## 1. 测试对象

| 项 | 当前值 |
| --- | --- |
| 产品版本 | `0.1.0`（package/Tauri/Cargo） |
| 工作区 | `D:\AIProjects\CodexProjects\Gulugulu\main` |
| 分支 | `codex/onboarding-fusion-factory-flow` |
| HEAD | `6dcadf2`（2026-08-07 合并日常运维分支） |
| 工作树 | 非干净；22 个已跟踪文件修改、1 个未跟踪引导文件 |
| 当前包定义 | 本地工作树快照，不等同于 GitHub Release 或 Steam 已发布版本 |

正式执行前必须冻结新的提交/SHA 或保存完整补丁指纹。若这些未提交改动继续变化，本文件只能作为风险审阅，不能作为测试结果的唯一版本证明。

## 2. 与上一个干净 HEAD 的玩家风险差异

本次仅依据版本控制变更清单、产品文档和玩家可见页面做黑盒风险映射，不以源码实现推导通过标准。

| 变更域 | 可见风险 | 用例调整 |
| --- | --- | --- |
| 新手强引导 | 当前首屏显示 `1/63`；包含跳过确认、更多后院/融合教学 | 新增 63 步顺从/急躁/中断/逆序/跳过/重启矩阵 |
| 后院与孵化 | 后院场景、近身操作、孵化坑位发生变化 | 增加走位边界、目标重叠、收蛋/陪伴/融合竞态 |
| 融合与异步生成 | 本地融合、AI/Steam 等待和恢复路径发生变化 | 增加无 Codex/Claude、Steam 断连、超时晚到、重启与幂等恢复 |
| Steam 同步 | Steam 状态/同步逻辑发生变化 | 增加库存对账、离线恢复、重复响应、云/本地并发 |
| 工厂设施/流程 | 工厂设施逻辑有变更 | 保留原 200 角色、结算、商店、贷款、竞态和恢复全回归 |
| 多语言 | 运行时语言包与人工覆盖变更量大 | 21 语种全界面重新截图；不能只跑静态 i18n 检查 |

## 3. 当前测试运力

| 能力 | 状态 | 边界 |
| --- | --- | --- |
| Windows 主机 | Windows 11 专业版 64 位，Build 26200 | 当前无 Windows 10 实机 |
| GPU | NVIDIA GeForce RTX 4070 Ti SUPER | 非低配代表机 |
| Node/npm | Node 24.13.0，npm 11.6.2 | 可执行现有浏览器/脚本测试 |
| Rust | cargo/rustc 1.95.0 | 可执行源码级补充测试，但不能代替黑盒 |
| Codex/Claude | 两者均已安装 | 当前不能直接证明“完全未安装”场景，需隔离 PATH/测试用户 |
| Steam | 客户端正在运行 | 需另备离线、未登录、空库存、双机云冲突测试条件 |
| Git/GitHub CLI | 已安装 | 只服务版本与交付，不属于玩家验证 |
| 浏览器黑盒 | 可在 `127.0.0.1:5250` 查看当前页面 | 无原生托盘、全局键盘钩子、Steam IPC 证明力 |
| 桌面自动化 | 当前插件运行依赖缺失，初始化失败 | Tauri 原生可见全屏检查本轮标 `BLOCKED-CAPACITY`，需修复工具或人工补测 |
| macOS/Linux | 无 | 平台用例标 `BLOCKED-CAPACITY` |

## 4. 已存在的补充自动化资产

现有脚本覆盖工厂性能/布局/本地化/首次运行/竞态/经济长跑、引导中断、发布边界、i18n、图鉴、皮肤、Steam 成就/物品定义、开机启动与点击穿透等。历史 `test/auto_test` 有 21 轮工厂和界面证据。

这些资产可用于：快速发现回归、生成视觉状态、压力输入和修复后对拍。它们不能单独关闭以下黑盒风险：安装/升级、真实 Tauri 窗口、托盘、全局键盘输入、Steam 客户端/库存/云、操作系统通知、真实文件锁/磁盘满和多机冲突。

## 5. 本轮准入探查事实

- 当前浏览器玩家首屏可见“破壳入职 `1/63`”，可点击教学蛋进入 `2/63`，点击首宠进入 `3/63`。
- “跳过整个新手引导”先出现确认对话框，确认后退出引导并保留正常宠物界面。
- Windows TCP 排除段包含 `5141–5240`，所以默认开发端口 `5173` 和备用 `5174` 均以 `EACCES` 失败；显式使用 `5250` 后启动成功。
- 当前已有一个 Tauri `gulugulu` 进程运行，但因桌面控制能力缺失，本轮探查没有把原生窗口状态记为通过。

## 6. 当前缺口与执行前动作

1. 冻结当前工作树并生成候选安装包 SHA-256；确认测试的是本地候选、GitHub Release 还是 Steam 分支。
2. 修复或替换桌面自动化能力，或安排人工原生 Tauri 截图/录像席位。
3. 准备 Windows 10、macOS、Linux、低配 GPU 和 200% DPI 席位。
4. 准备 Steam 测试账号：未登录、空库存、丰富库存、云存档双机；确认不会碰真实价值库存。
5. 准备隔离用户环境以模拟 Codex/Claude 均缺失、各缺一个、日志无权限。
6. 建立合法游玩存档包：新档、引导各阶段、两只满级、待收融合蛋、丰富图鉴、工厂中局、云冲突。
7. 为 19 种非简中/英文语言安排母语或目标语言审校；否则标 `NEEDS-NATIVE-REVIEW`。

## 7. 当前用例集调整结论

- 新增：63 步引导、跳过确认、中断恢复、乱序自愈。
- 新增：无 Agent 仍能本地融合/兜底、Steam 等待超时和人工恢复、响应晚到幂等。
- 新增：端口被占用与被系统排除两种失败模型。
- 扩展：存档/云/Steam 库存的四提交点强退矩阵。
- 扩展：21 语种全界面视觉与目标语言任务理解矩阵。
- 保留：工厂 200 角色、结算/商店/贷款连点、减少动态、首轮恢复、路线与覆盖层测试。
- 降级为补充证据：所有读取实现或依赖调试注入的脚本结果。
